{
  "name": "mybudget",
  "version": "1.0.0",
  "main": "",
  "scripts": {
      "start": "npm start --prefix server",
      "install-client": "cd frontend && npm install && npm run start",
      "install-server": "cd backend && npm install && npm run build",
      "heroku-postbuild": "npm run install-client && npm run install-server"
  }
}